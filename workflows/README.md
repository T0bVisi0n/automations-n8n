# Workflows

This folder stores exported n8n workflows.

## Rules

- One file per workflow export
- Use clear names
- Add documentation in `/docs` if workflow logic is not obvious
- Do not store secrets in workflow files if avoidable
